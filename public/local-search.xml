<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Hello world!</title>
    <link href="/2026/03/26/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C-%E4%BC%A0%E8%BE%93%E5%B1%82/"/>
    <url>/2026/03/26/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C-%E4%BC%A0%E8%BE%93%E5%B1%82/</url>
    
    <content type="html"><![CDATA[<h1 id="传输层概述"><a href="#传输层概述" class="headerlink" title="传输层概述"></a>传输层概述</h1><ul><li>传输层是主机才有的层次，路由器最多能够到达网络层</li><li>传输层，为运行在不同主机上的进程之间提供了逻辑通信，网络层提供主机之间的逻辑通信</li><li>只有主机的协议栈才有传输层和应用层<h1 id="传输层的功能"><a href="#传输层的功能" class="headerlink" title="传输层的功能"></a>传输层的功能</h1><h2 id="提供进程和进程之间的逻辑通信"><a href="#提供进程和进程之间的逻辑通信" class="headerlink" title="提供进程和进程之间的逻辑通信"></a>提供进程和进程之间的逻辑通信</h2>网络层为主机提供逻辑通信，传输层为主机上的进程提供端到端的逻辑通信<h2 id="复用和分用"><a href="#复用和分用" class="headerlink" title="复用和分用"></a>复用和分用</h2></li><li>复用：发送方不同的应用进程都可以用同一个传输层协议传送数据</li><li>分用：接收放的传输层在剥去报文的首部能够把这些数据正确交付到目的应用进程<h2 id="差错检测"><a href="#差错检测" class="headerlink" title="差错检测"></a>差错检测</h2></li><li>网络层中，只校验首部是否出差错而不检查数据部分</li><li>传输层需要对收到的报文进行差错检测<h2 id="提供两种不同的传输协议"><a href="#提供两种不同的传输协议" class="headerlink" title="提供两种不同的传输协议"></a>提供两种不同的传输协议</h2></li><li>面向连接的TCP协议</li><li>无连接的UDP协议<h1 id="传输层的寻址和端口"><a href="#传输层的寻址和端口" class="headerlink" title="传输层的寻址和端口"></a>传输层的寻址和端口</h1></li><li>传输层使用端口来标识主机中的应用进程</li><li>端口值具有本地意义，只是为了标志本计算机应用层中各个进程在和传输层交互时的层间接口</li><li>不同计算机的相同端口是没有联系的</li><li>端口号长度16bit，能标识65534个不同的端口号<h2 id="端口的分类"><a href="#端口的分类" class="headerlink" title="端口的分类"></a>端口的分类</h2></li><li>服务器端使用的端口号<ul><li>熟知端口号(0-1023）</li><li>登记端口号(1024-49151)</li></ul></li><li>客户端使用的端口号(49152-695535)</li></ul><table><thead><tr><th>FTP</th><th>TELNET</th><th>SMTP</th><th>DNS</th><th>TFTP</th><th>HTTP</th><th>SNMP</th></tr></thead><tbody><tr><td>21</td><td>23</td><td>25</td><td>53</td><td>69</td><td>80</td><td>161</td></tr></tbody></table><h1 id="套接字"><a href="#套接字" class="headerlink" title="套接字"></a>套接字</h1><ul><li>socket = （IP地址：端口号）</li><li>套接字唯一地标识网络中的一台主机和其上的一个应用（进程）<h1 id="UDP协议"><a href="#UDP协议" class="headerlink" title="UDP协议"></a>UDP协议</h1>用户数据报协议UDP（User Datagram Protocol） = IP的数据报服务 + （复用，分用，差错检测）<h2 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h2></li><li>UDP是无连接的</li><li>UDP使用尽最大努力交付，不保证可靠交付</li><li>UDP没有拥塞控制，有利于实时应用（视频，语音）</li><li>UDP分组首部开销小，TCP有20B的首部开销，UDP仅有8B的开销</li><li>UDP是面向报文的，报文是UDP数据报处理的最小单位</li></ul><p><img src="https://github.com/user-attachments/assets/68e304d4-6df7-447d-a874-3d07504f3bee" alt="Pasted image 20240527233244"></p><ul><li>报文太长，UDP把报文交给IP层后，会导致分片</li><li>报文太短，UDP把它交给IP层后，会使IP数据报的首部相对长度太大</li><li>两者都会减低IP层的效率</li></ul><p><strong>==UDP校验和校验出UDP数据报是错误的，可以丢弃，也可以交付给上层，但是要附上错误报告==</strong></p><h2 id="UDP首部格式"><a href="#UDP首部格式" class="headerlink" title="UDP首部格式"></a>UDP首部格式</h2><p><img src="https://github.com/user-attachments/assets/63516269-0013-4cb2-bb03-7d59adf51806" alt="Pasted image 20240527233455"></p><ul><li>用户数据报UDP有两个字段：数据字段和首部字段。</li><li>首部字段只有8个字节，由四个字段组成，每个字段长度都是两个字节</li></ul><table><thead><tr><th>源端口</th><th>目的端口</th><th>长度</th><th>校验和</th></tr></thead><tbody><tr><td>不是必须的，只有在需要对方回信时选用，不需要时可用全0</td><td>在终点交付报文时必须使用</td><td>UDP数据报的长度，首部和数据部分长度之和，其最小值是8</td><td>检测UDP在传输中是否出错，如果有错就丢弃</td></tr></tbody></table><h2 id="UDP基于端口的分用步骤"><a href="#UDP基于端口的分用步骤" class="headerlink" title="UDP基于端口的分用步骤"></a>UDP基于端口的分用步骤</h2><ul><li>当运输层IP层收到UDP数据报时，就根据首部中的目的端口，把UDP数据报通过相应的端口交付给上层应用进程</li><li>如果接收方UDP发现收到的报文的目的端口号不正确（即不存在对应于该端口号的应用进程），即丢弃该报文</li><li>然后由网际控制报文协议ICMP发送“端口不可达”差错报文给发送方。<h2 id="UDP校验"><a href="#UDP校验" class="headerlink" title="UDP校验"></a>UDP校验</h2></li><li> UDP检验和提供差错检测功能。在计算校验和时，要在UDP用户数据报之前增加12字节的伪首部</li><li>伪首部既不向下传送也不向上递交，只是为了计算校验和</li></ul><p><img src="https://github.com/user-attachments/assets/57edadf0-f50d-4900-9443-ac2eca66ac5c" alt="Pasted image 20240527233836"></p><ul><li>源IP地址和目的IP地址：和IP数据一样，各占4个字节。</li><li>伪首部第3个字段是全零。</li><li>协议字段：UDP协议的协议字段值是17。</li><li>UDP长度：UDP用户数据报长度，首部长度和数据部分长度之和。</li></ul><p><img src="https://github.com/user-attachments/assets/ff926be8-c446-42ca-b92f-935ed53fa2b3" alt="Pasted image 20240527234019"></p><ul><li>将校验和字段置位0。</li><li>将伪首部和UDP用户数据报（首部和数据部分）看成是以16位为单位的二进制组成，依次进行二进制反码求和。</li><li>将求和的结果的反码写入校验和字段。<h1 id="TCP"><a href="#TCP" class="headerlink" title="TCP"></a>TCP</h1><h2 id="特点-1"><a href="#特点-1" class="headerlink" title="特点"></a>特点</h2><blockquote><ul><li>TCP是面向连接的传输层协议</li><li>TCP连接是一条虚电路（逻辑电路），不是一条真正的物理连接</li><li>每条TCP连接只能有两个端点，每条TCP连接只能是点对点的</li><li>TCP连接的端点叫做套接字（ socket ）或插口</li></ul></blockquote></li></ul><p><strong>TCP提供可靠交付的服务，保证数据无差错，不丢失，不重复，并且按序到达</strong></p><ul><li>TCP提供全双工通信<ul><li>发送缓存：用于存放准备发送的数据和已发送但未收到确认的数据</li><li>接受缓存：用于存放按序到达但尚未被接受应用程序读取的数据和不按序到达的数据</li></ul></li><li>TCP面向字节流，TCP把应用程序交下来的数据仅视作一连串的无结构的字节流</li><li>TCP确认号是期待收到对方下一个报文段的第一个字节的序号<h2 id="TCP报文段"><a href="#TCP报文段" class="headerlink" title="TCP报文段"></a>TCP报文段</h2><img src="https://github.com/user-attachments/assets/8300203b-5921-470b-8fc8-b60cea41b51e" alt="Pasted image 20240527234553"></li><li>一个TCP报文段 = 首部 + 数据部分</li><li>首部的前20个字节是固定的</li><li>后面有4N字节是根据需要而增加的选项</li><li>TCP首部的最小字节是20字节</li></ul><p><img src="https://github.com/user-attachments/assets/db820985-c8d5-4dfa-a6ea-139c149fd9aa" alt="Pasted image 20240527234745"></p><table><thead><tr><th>源端口和目的端口</th><th>TCP分用功能的实现</th></tr></thead><tbody><tr><td><strong>序号</strong></td><td>报文段所发送的数据的第一个字节的序号</td></tr><tr><td><strong>确认号</strong></td><td>期望收到下个报文段第一个数据字节的序号</td></tr><tr><td><strong>数据偏移/首部长度</strong></td><td>期望收到下个报文段第一个数据字节的序号</td></tr><tr><td><strong>保留</strong></td><td>占6位，保留为今后使用</td></tr><tr><td><strong>紧急位URG</strong></td><td>表明报文段中有紧急数据</td></tr><tr><td><strong>确认位ACK</strong></td><td>仅当ACK=1确认号才有效</td></tr><tr><td><strong>推送位PSH</strong></td><td>实际很少使用</td></tr><tr><td><strong>复位位RST</strong></td><td>TCP连接出现差错，需释放连接重新建立</td></tr><tr><td><strong>同步位SYN</strong></td><td>在连接建立时用来同步序号</td></tr><tr><td><strong>终止位FIN</strong></td><td>用来释放一个连接，FIN=1表明报文段的发送方数据已发送完毕，并要求释放运输连接</td></tr><tr><td><strong>窗口</strong></td><td>作为接收方让发送方设置其发送窗口的依据</td></tr><tr><td><strong>校验和</strong></td><td>校验首部和数据部分，需要伪首部</td></tr><tr><td><strong>紧急指针</strong></td><td>URG=1时表示报文段中紧急数据的字节数</td></tr><tr><td><strong>选项</strong></td><td>最大报文长度MSS：TCP报文段数据部分的长度<br><br>扩大窗口<br><br>时间戳</td></tr><tr><td><strong>填充</strong></td><td>可变部分</td></tr></tbody></table><h2 id="TCP连接（重点）"><a href="#TCP连接（重点）" class="headerlink" title="TCP连接（重点）"></a>TCP连接（重点）</h2><p><img src="https://github.com/user-attachments/assets/07ecc824-e0a1-454f-b16a-6dbc49f98878" alt="Pasted image 20240527235519"></p><h3 id="第一次握手"><a href="#第一次握手" class="headerlink" title="第一次握手"></a>第一次握手</h3><ul><li>客户端向服务端发送请求，首部同步位SYN=1，选择一个初始序号seq=x</li><li>SYN报文段不携带数据，序号消耗一个序号</li><li>客户进程进入SYN-SENT（同步已发送）状态<h3 id="第二次握手"><a href="#第二次握手" class="headerlink" title="第二次握手"></a>第二次握手</h3></li><li>服务器收到SYN报文—&gt;同意连接—-&gt;服务器为TCP连接分配缓存和变量—-&gt;向客户端返回确认报文段</li><li>同步位SYN=1，确认位ACK=1，确认号ack=x+1，为自己选择一个初始序号seq=y【随便给】</li><li>确认报文段不能携带数据，需要消耗一个序号</li><li>服务器进程进入SYN-RCVD（同步收到）状态<h3 id="第三次握手"><a href="#第三次握手" class="headerlink" title="第三次握手"></a>第三次握手</h3></li><li>客户进程收到服务器进程的确认报文—&gt;客户端为TCP连接分配缓存和变量</li><li>向服务器端返回一个报文段，对服务器确认报文进行确认</li><li>报文中ACK=1，确认号seq=y+1，自己序号seq=x+1</li><li>客户端进入ESTABLISHED（已建立连接）状态</li><li>此时TCP连接已经建立，服务器收到客户端的确认后，进入establish状态<h2 id="TCP连接释放（四次挥手）"><a href="#TCP连接释放（四次挥手）" class="headerlink" title="TCP连接释放（四次挥手）"></a>TCP连接释放（四次挥手）</h2><img src="https://github.com/user-attachments/assets/ff42119e-68a4-4695-a955-fdce2c6a8710" alt="Pasted image 20240527235812"><h3 id="第一次挥手"><a href="#第一次挥手" class="headerlink" title="第一次挥手"></a>第一次挥手</h3></li><li>客户端向服务端发出连接释放报文段—–&gt;停止发送数据，主动关闭连接</li><li>报文中终止控制FIN=1，序号seq=u（值等于前面已传送的数据的最后一个字节的序号+1）</li><li>客户端进入FIN-WAIT-1（终止等待1状态）<h3 id="第二次挥手"><a href="#第二次挥手" class="headerlink" title="第二次挥手"></a>第二次挥手</h3></li><li>服务器收到连接释放报文段后发出确认</li><li>确认位ACK=1，确认号ACK=u+1，序号seq=v（值等于服务器前面已传送过的数据最后一个字节的序号+1）</li><li>服务器进入CLOSE-WAIT（关闭等待）状态</li><li>此时TCP连接处于半关闭状态，客户端到数据段的连接释放，但是服务器还可以向客户端发送文件，且客户端需接受</li><li>客服端收到服务器的确认—-&gt;进入FIN-WAIT-2（终止等待2）状态，等待服务器发出的连接释放报文段<h3 id="第三次挥手"><a href="#第三次挥手" class="headerlink" title="第三次挥手"></a>第三次挥手</h3></li><li>服务器发出连接释放报文段，FIN=1，报文序号seq=w（在半关闭状态下服务器可能又发送了一些数据）</li><li>服务器重复上次已发送到确认号ack=u+1</li><li>服务器进入LAST-ACK（最后确认状态），等待客户端的确认<h3 id="第四次挥手"><a href="#第四次挥手" class="headerlink" title="第四次挥手"></a>第四次挥手</h3></li><li>客户端收到服务器端发出的连接释放报文—–&gt;对此发出确认，将确认位ACK置为1，确认号ack=w+1，序号seq=u+1</li><li>客户端进入TIME-WAIT（时间等待状态）</li><li>在经过时间等待计时器设置的世界2MSL（最大报文段寿命）后，客户端进入CLOSE(关闭）状态<h1 id="TCP可靠传输"><a href="#TCP可靠传输" class="headerlink" title="TCP可靠传输"></a>TCP可靠传输</h1></li><li>TCP提供的可靠数据传输保证接收方进程从缓存区读出的字节流与发送方发出的字节流完全一样</li><li>TCP使用校验（与UDP一致），序号，确认，重传来达到这个目的<h2 id="序号"><a href="#序号" class="headerlink" title="序号"></a>序号</h2></li><li>TCP首部的序号字段用来保证数据能有序提交给应用层</li><li>TCP把数据视作一个无结构但有序的字节流</li><li>序号建立在传送的字节流之上，而不建立在报文段之上</li><li>序号字段的值是指本报文段所发送的数据的第一个字节的序号<h2 id="确认"><a href="#确认" class="headerlink" title="确认"></a>确认</h2></li><li>TCP首部的确认号是期望收到对方的下一个报文段的数据的第一个字节的序号</li><li>TCP使用默认累计<h2 id="重传"><a href="#重传" class="headerlink" title="重传"></a>重传</h2><h3 id="超时"><a href="#超时" class="headerlink" title="超时"></a>超时</h3></li><li>计时器设置的重传时间到期但还未收到确认时，就要重传这一报文段</li><li>发出事件和收到确认时间之差 = 报文段的往返时间RTT</li><li>超时存在的问题：超时周期问问太长<h3 id="冗余ACK"><a href="#冗余ACK" class="headerlink" title="冗余ACK"></a>冗余ACK</h3></li><li>冗余ACK就是再次确认某个报文段的ACK，而发送方先前已经收到过该报文段的确认</li><li>TCP规定每当比期望序号大的失序报文到达时，就发送一个冗余ACK，指明下一个期待字节的序号</li><li>TCP规定当发送方收到对同一个报文段的3个冗余ACK时，就可以认为跟在这个被确认报文段之后的报文段已经丢失</li><li>快速重传：只要某个报文段丢失，就立即重传<h1 id="TCP流量控制"><a href="#TCP流量控制" class="headerlink" title="TCP流量控制"></a>TCP流量控制</h1></li><li>流量控制的目的：使发送方的发送速率与接收方应用程序的读取速率相匹配</li><li>判断网络拥塞的依据就是超时</li><li>TCP利用滑动窗口机制实现对发送方的流量控制</li><li>TCP的窗口单位是字节</li><li>发送窗口的上限值=min[rwnd, cwnd]</li></ul><table><thead><tr><th>拥塞窗口cwnd</th><th>接收窗口rwnd</th></tr></thead><tbody><tr><td>发送方根据当前网络拥塞程度估计而确认的窗口值</td><td>接收方根据自己接收缓存的大小，动态地调整发送方的发送</td></tr></tbody></table><h1 id="TCP拥塞控制算法"><a href="#TCP拥塞控制算法" class="headerlink" title="TCP拥塞控制算法"></a>TCP拥塞控制算法</h1><ul><li>拥塞控制是指防止过多的数据注入网络，保证网络中的路由器或链路不致过载</li><li>拥塞往往表现为通信时延的增加<h2 id="慢开始"><a href="#慢开始" class="headerlink" title="慢开始"></a>慢开始</h2><img src="https://github.com/user-attachments/assets/3a76b14f-8757-4c62-8f67-f0fcfdf75ef4" alt="Pasted image 20240528135517"></li><li>cwnd &lt; ssthresh时，使用慢开始算法</li><li>在达到慢开始门限ssthresh前，cwnd按指数规律增长<h2 id="拥塞避免"><a href="#拥塞避免" class="headerlink" title="拥塞避免"></a>拥塞避免</h2></li><li>cwnd &gt; ssthresh时，使用拥塞避免算法</li><li>cwnd每次增加1</li><li>出现乘法减小时，ssthresh设置为当前cwnd的一半，cwnd设置为1，然后重新慢开始<h2 id="快恢复，快重传"><a href="#快恢复，快重传" class="headerlink" title="快恢复，快重传"></a>快恢复，快重传</h2><img src="https://github.com/user-attachments/assets/89b4d1bd-8f95-4ede-9fdd-0adc07079ef0" alt="Pasted image 20240528135638"><h3 id="快重传"><a href="#快重传" class="headerlink" title="快重传"></a>快重传</h3></li><li>当发送方连续收到3个重复的ack报文时，直接重传对方尚未收到的报文段</li><li>不必等待那个报文段设置的重传计时器超时<h3 id="快恢复"><a href="#快恢复" class="headerlink" title="快恢复"></a>快恢复</h3></li><li>当发送方连续收到3个冗余ack时，执行“乘法减小”算法</li><li>把ssthresh设置为当前cwnd的一半，然后cwnd从ssthresh开始使用拥塞避免算法</li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Tower of Hanoi</title>
    <link href="/2026/03/24/tower-of-hanoi/"/>
    <url>/2026/03/24/tower-of-hanoi/</url>
    
    <content type="html"><![CDATA[<blockquote></blockquote>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hello world!</title>
    <link href="/2026/03/24/hello-world-1/"/>
    <url>/2026/03/24/hello-world-1/</url>
    
    <content type="html"><![CDATA[<p>T﻿his is a text to test!</p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
